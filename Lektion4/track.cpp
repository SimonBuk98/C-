#include "track.h"

/*Class definitions*/

Track::Track(std::string title, int length): title(title), length(length){
}
std::string Track::getTitle(){
  return title;
}
int Trak::getLength(){
  return length; 
}

